package com.dumbdogdiner.sass.api.store.statistic;

/**
 * Represents the global group for statistics which have only one value. Prefer
 * using this over other unit or singleton types.
 */
public enum GlobalGroup {
    /**
     * The only instance of this object.
     */
    INSTANCE
}
